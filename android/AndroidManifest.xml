<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="at.valli.savage"
          android:versionCode="4"
          android:versionName="1.4">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="8" />

    <application android:label="@string/app_name" android:icon="@drawable/icon">
        <activity android:name=".activity.Main"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.Connect" />
        <activity android:name=".activity.Command"/>
        <activity android:name=".activity.SaveEdit"/>
        <activity android:name=".activity.Bookmarks"/>
        <activity android:name=".activity.About"/>
    </application>
</manifest>
